# Tasks

## 文档重构

- [ ] Task 1: 重构实施方案文档结构
  - 按 spec.md 中定义的章节结构重新组织现有内容
  - 将分散的 UI 设计、部署方案、降级策略等内容归入对应章节
  - 统一章节编号，确保标题层级正确
  - 移除迭代讨论痕迹（如"之前 vs 之后"对比），保留最终决策
  - 确保每个章节内容完整、独立可读

- [ ] Task 2: 新增 LLM 厂商模板配置设计章节
  - 在文档 4.2 问答引擎（LLM 网关部分）新增厂商模板系统设计
  - 包含：厂商模板数据结构、自动填充逻辑、自定义模式、多模型降级配置
  - 包含：设置页 UI 的厂商选择交互描述
  - 更新 LLM 网关代码示例：从硬编码改为配置驱动

## LLM 配置设计

- [ ] Task 3: 设计厂商模板数据结构
  - 定义 `ProviderTemplate` 模型（厂商名称、base_url、默认模型列表、是否需要 API Key、图标等）
  - 定义 `LLMConfig` 模型（主模型配置、降级模型配置、用户自定义模型列表）
  - 内置 7 个厂商模板 + 1 个自定义占位模板

- [ ] Task 4: 设计 LLM 配置页 UI
  - 在设置页中新增 LLM 配置区域
  - 厂商选择下拉框（云端/本地分组，带图标）
  - 选择厂商后自动填充字段，API Key 由用户手动输入
  - "自定义"模式：所有字段空白，用户自行填写
  - 主模型 + 降级模型双配置区域
  - 测试连接按钮（发送测试消息验证连通性）

# Task Dependencies

- Task 2 依赖 Task 3（数据结构定义后才能写设计文档）
- Task 1、Task 3 可并行执行
- Task 4 依赖 Task 3
services:
  zhida-engine:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:18900:18900"
    # .env 不提交到 Git。它存放域名、会话密钥及其他部署私有配置。
    env_file:
      - .env
    environment:
      ZHIDA_API_HOST: "0.0.0.0"
      ZHIDA_API_PORT: "18900"
      ZHIDA_DATA_DIR: "/data"
      ZHIDA_EMBEDDING_MODE: "cloud"
    volumes:
      - ./data:/data
